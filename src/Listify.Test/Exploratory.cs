namespace Listify.Test;

public class Exploratory {

    [Fact]
    public void Exploratory_test_just_asserts_true() {
        // This is done to just test that all of the pipeline and runners and so on are working, its not a real test obvs.
        Assert.True(true);
    }
}